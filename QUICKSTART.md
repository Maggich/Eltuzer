# Быстрый старт - El Tuzer

## Шаг 1: Запуск проекта

```bash
docker-compose up --build
```

Подождите, пока все контейнеры запустятся (это может занять несколько минут при первом запуске).

## Шаг 2: Создание администратора

Откройте новый терминал и выполните:

```bash
docker-compose exec backend python create_admin.py
```

Используйте значения по умолчанию или введите свои:
- Имя пользователя: `admin` (или свое)
- Email: `admin@eltuzer.com` (или свой)
- Пароль: `admin123` (или свой)

## Шаг 3: Откройте сайт

- **Главная страница**: http://localhost:3000
- **Вход в панель управления**: http://localhost:3000/login
- **API документация**: http://localhost:8001/docs

## Шаг 4: Войдите в панель управления

**Данные для входа:**
- **Логин**: `admin`
- **Пароль**: `admin123`

1. Перейдите на http://localhost:3000/login
2. Введите логин и пароль
3. Начните добавлять категории и товары!

## Полезные команды

### Остановить проект
```bash
docker-compose down
```

### Остановить и удалить все данные
```bash
docker-compose down -v
```

### Просмотр логов
```bash
docker-compose logs -f
```

### Перезапуск одного сервиса
```bash
docker-compose restart backend
# или
docker-compose restart frontend
```

## Решение проблем

### Порт уже занят
Если порты 3000 или 8000 заняты, измените их в `docker-compose.yml`:
```yaml
ports:
  - "3001:3000"  # вместо 3000:3000
```

### Ошибка подключения к базе данных
Подождите несколько секунд после запуска - база данных инициализируется.

### Изображения не загружаются
Убедитесь, что папка `backend/uploads` существует и доступна для записи.

## Следующие шаги

1. Добавьте категории товаров в админ-панели
2. Добавьте товары с изображениями
3. Настройте дизайн под ваши нужды
4. Измените пароль администратора на более безопасный

Удачи! 🚀

